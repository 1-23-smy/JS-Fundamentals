const myarr=[1,2,4,5,6];


console.log("slice method");
console.log(myarr.slice(1,3));
console.log("splice method");
console.log(myarr.splice(1,3));
console.log(myarr);
//splice method change the original array where as the slice not changes the original array.
//splice method has the access to the memory allocation where as the slice method has not the access to the memory allocation.

